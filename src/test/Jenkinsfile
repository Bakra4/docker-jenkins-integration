pipeline {
    agent any

    stages {
        stage("build") {
            steps {
            echo 'Build Step Completed'}
        }
    }
    stages {
            stage("test") {
                steps {
                echo 'Testing Step Completed'}
            }
        }
        stages {
                    stage("deploy") {
                        steps {
                        echo 'Deploy Step Completed'}
                    }
                }
        }
}